function mostrar() {
		// Generar un número aleatorio entre 1 y 10
		var numeroRandom = Math.floor(Math.random() * 10) + 1;
	
		// Mostrar el número en un alerta
		alert("El número random generado es: " + numeroRandom);
	}
	//FIN DE LA FUNCIÓN